# Copiar a .env y ajustar. No subir .env al repositorio.
# Variable obligatoria: URL base del API del backend.
# Si no se define, el frontend fallará al iniciar.
# En Railway se usa también durante el build del Dockerfile.
VITE_API_URL=http://localhost:8081/api
